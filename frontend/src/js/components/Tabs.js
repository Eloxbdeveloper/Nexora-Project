// TODO: Tabs Mapa / Reportes
export function initTabs() {
  // document.querySelectorAll('.tab-btn').forEach(btn => {
  //   btn.addEventListener('click', () => switchTab(btn.dataset.tab));
  // });
}
function switchTab(tabId) { }