import { Module } from '@nestjs/common';

// TODO: implementar en la historia de usuario correspondiente a usuarios
// (schema, dto, service, controller — mismo patrón que incidents.module.ts)
@Module({})
export class UsersModule {}
